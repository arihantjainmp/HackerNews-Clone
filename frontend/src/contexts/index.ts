/**
 * Contexts barrel export
 * Provides convenient imports for all context providers and hooks
 */

export { AuthProvider, useAuth } from './AuthContext';
export { default as AuthContext } from './AuthContext';
